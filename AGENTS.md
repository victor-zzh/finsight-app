# 智能财报分析与风险识别应用 - 项目详细说明

## 1. 项目愿景 (Project Vision)

本项目旨在为个人投资者打造一款严谨、智能、可追溯的财报分析与风险识别工具。通过结合确定性的数据分析与先进的AI生成能力，我们致力于解决个人投资者在信息处理、专业分析和风险洞察方面的核心痛痛点，让复杂的财报分析变得简单、可靠且富有洞察力。

---

## 2. 核心架构哲学 (Core Architecture Philosophy)

为确保分析结论的精确性与可靠性，应用遵循**“混合式智能分析架构” (Hybrid Intelligence Architecture)**，其核心原则如下：

* **计算与解读分离：** 严谨的财务比率计算、数据趋势分析等确定性任务由代码完成；语义理解、归纳总结、报告生成等任务交由大语言模型（LLM）处理。
* **事实与观点分离：** 应用的所有结论必须基于可追溯的数据源（财报原文、结构化财务数据）。LLM的作用是“基于事实的总结”，而非“凭空创造观点”。
* **在线与离线分离：** 用户实时查询服务（在线）与繁重的数据处理任务（离线）在架构上解耦，确保系统性能与稳定性。

---

## 3. 功能规划详情 (Feature Breakdown)

#### 3.1 核心财报分析引擎

* **功能描述：** 提供基于结构化财报数据的深度定量分析。这是整个应用的数据基石。
* **实现细节：**
    * 通过独立的Python脚本，定时从`Tushare`获取A股上市公司历年的资产负债表、利润表、现金流量表等核心财务数据。
    * 数据经过清洗和标准化后，存入`PostgreSQL`数据库。
    * 后端内置“定量分析引擎”，可根据请求即时计算关键财务比率（如ROE、资产负债率、毛利率等）及其历史趋势。
    * 内置“风险规则库”，根据财务指标自动触发预警（如：连续三年经营现金流低于净利润）。

#### 3.2 RAG 深度问答系统

* **功能描述：** 允许用户使用自然语言，对财报中的非结构化文本内容（如“管理层讨论与分析”）进行提问，理解数据背后的“为什么”。
* **实现细节：**
    * Python脚本从巨潮资讯网等官方渠道下载财报PDF原文。
    * 使用`PyMuPDF`解析PDF，并进行语义切块（Chunking）。
    * 调用Embedding模型将文本块向量化，与原文、元数据（股票代码、年份、页码）一同存储至`Qdrant Cloud`向量数据库。
    * 后端API接收用户问题后，将其向量化并在Qdrant中进行带元数据过滤的精确语义检索，找到最相关的原文片段作为LLM的上下文。

#### 3.3 实时行情与新闻集成

* **功能描述：** 为深度分析提供实时的“价格锚点”和“市场情绪”快照，捕捉基本面之外的突发风险与机遇。
* **实现细节：**
    * Next.js后端提供一个代理API（如`/api/realtime-quote`）。
    * 该API实时调用`Tushare`或`AKshare`的行情接口，获取最新股价、成交量、动态市盈率（PE-TTM）等。
    * 后端使用缓存机制（如Redis或内存缓存）降低对上游数据源的请求频率。
    * 最终分析报告可集成最新的市场数据，使结论更具时效性。

#### 3.4 智能引导式分析 (Smart Tags)

* **功能描述：** 在用户完成一次分析后，系统根据当前上下文，动态生成“智能标签”，引导用户进行下一步的追问与探索，形成分析飞轮。
* **实现细节：**
    * **示例：** 用户分析完“贵州茅台近五年财报”后，系统自动在输入框上方生成 `[查看实时行情]` `[分析估值水平]` `[对比五粮液]` 等标签。
    * 后端API在返回分析结果的同时，会额外返回一个`suggestions`数组，包含推荐的标签和对应的查询语句。
    * 前端动态渲染这些标签，用户点击即可发起一次新的、更聚焦的分析请求。

#### 3.5 结论可追溯与原文下载

* **功能描述：** 应用内的每一条分析结论都必须是可验证的。为满足用户的深度核实需求，提供财报原文的下载功能。
* **实现细节：**
    * Python脚本在下载财报PDF后，会将其存储在云对象存储（如阿里云OSS）中。
    * PostgreSQL数据库中记录每个PDF的元数据及其存储路径。
    * Next.js后端提供一个安全的下载API，根据用户请求生成一个有时效性的、安全的下载链接（pre-signed URL），供前端下载。

---

## 4. 技术选型 (Technology Stack)

| 模块 | 技术选型 | 理由 |
| :--- | :--- | :--- |
| **前端** | **Next.js (App Router)** | 全栈框架，提供优秀的开发体验和性能（SSR/SSG）。与后端API无缝集成。 |
| **后端** | **Next.js API Routes (TypeScript)** | 与前端同构，简化开发。作为安全中间层，处理用户请求、连接数据库和调用AI模型。 |
| **数据处理管道** | **Python 3.x** | 拥有最强大的数据科学库生态。用于爬虫、PDF解析、数据清洗和向量化。 |
| **关系型数据库** | **PostgreSQL** | 成熟、可靠的开源数据库。用于存储所有结构化数据：公司信息、财务指标、财报元数据等。 |
| **向量数据库** | **Qdrant Cloud** | 提供强大的元数据过滤能力，性能优异。SaaS服务简化了运维，且提供平滑迁移至自托管的选项。 |
| **AI 大语言模型** | **GLM (通用大模型)** | 作为应用的大脑，负责在**已有上下文**的基础上进行归纳、总结和语言生成。 |
| **部署 (建议)** | **Vercel / Docker** | Vercel用于部署Next.js应用；Docker容器化Python脚本，通过Cron Job或类似服务进行定时调度。 |

---

## 5. 核心工作流示例：分析“近五年财报”

此工作流展示了本应用“混合智能”的核心。

1.  **意图分析 (Intent Analysis):**
    * 后端API接收到用户问题：“分析贵州茅台近五年的盈利能力”。
    * 内置的“查询意图分析器”识别出关键词`盈利能力`和时间模式`近五年`，判断这是一个“混合驱动”的分析请求。
    * 2025/10/15 更新：
        * 意图分析改为由LLM（这段使用免费的GLM模型即可）来做识别，输出一段JSON字符串，包含分析类型（如“混合驱动”）、关键词（如`盈利能力`）和时间范围（如`近五年`）。

2.  **定量数据检索 (Quantitative Retrieval):**
    * 系统向 **PostgreSQL** 查询 `stock_code='600519'` 且 `year` 在近五年的`ROE`、`净利率`等盈利指标。
    * **产出：** 一份结构化的五年数据趋势JSON。

3.  **定性文本检索 (Qualitative Retrieval - RAG):**
    * 系统为每一年自动生成内部探测问题，如“2024年财报如何解释盈利变化？”。
    * 将问题向量化，并带着`Filter(year=2024, stock_code='600519')`的条件，向 **Qdrant Cloud** 进行精确搜索。
    * **产出：** 一个按年份组织的、包含财报原文关键段落的JSON。

4.  **综合分析生成 (LLM Synthesis):**
    * 系统将上述两份JSON（定量数据 + 定性原文）整合进一个精心设计的Prompt中。
    * 调用 **GLM**，指令其扮演财务分析师，结合数据和原文撰写一份有理有据的分析报告。
    * 最终将这份高质量的报告返回给前端。